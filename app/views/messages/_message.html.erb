<p id="<%= dom_id message %>" class="message">
  <time full_date="<%= message.created_at.strftime("%d %b %Y %H:%M:%S") %>"><%= message.created_at.strftime("%d/%m %H:%M") %></time> <strong><%= message.user&.name || message.guest_name || "Anonymous" %></strong>: <%= message.content %>
</p>
